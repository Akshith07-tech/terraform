# 🚀 Terraform EC2 with Nginx (AWS)

This project creates an **EC2 instance on AWS** using **Terraform**, provisions it with a key pair and security group, and installs **Nginx** using a startup script.

---

## 📁 Project Structure

```
terraform/
└── ec2/
    ├── ec2.tf
    ├── variables.tf
    ├── output.tf
    ├── terraform.tf
    ├── providers.tf
    ├── install_nginx.sh
    ├── .gitignore
    └── README.md
```

---

## ⚙️ 1️⃣ Configure AWS CLI

Before running Terraform, configure your AWS CLI with valid credentials:

```bash
aws configure
```

Enter:

- AWS Access Key ID  
- AWS Secret Access Key  
- Default region name (e.g. `ap-south-1`)  
- Default output format (`json`)

Verify your configuration:
```bash
aws configure list
```

---

## 🧱 2️⃣ Initialize Terraform

Initialize Terraform to download AWS provider plugins:
```bash
terraform init
```

---

## 🧩 3️⃣ Validate and Plan Infrastructure

Validate your configuration:
```bash
terraform validate
```

See what will be created:
```bash
terraform plan
```

---

## 🚀 4️⃣ Apply Configuration

Deploy the EC2 instance on AWS:
```bash
terraform apply -auto-approve
```

---

## 🌐 5️⃣ Connect to EC2 Instance

Once the instance is created, connect using SSH:
```bash
ssh -i terra-key-ec2 ubuntu@<public-ip>
```

🔑 Use the private key (`terra-key-ec2`) generated by Terraform.

---

## 🧰 6️⃣ Verify Nginx Installation

After SSH access, verify Nginx:
```bash
sudo systemctl status nginx
```

Or visit your instance’s public IP in a browser:
```
http://<public-ip>
```

You should see the **Nginx welcome page 🎉**

---

## 🧹 7️⃣ Destroy Infrastructure

When done, clean up all AWS resources:
```bash
terraform destroy -auto-approve
```

---

## 🧾 Notes

- Don’t push sensitive files like `.terraform/`, `terraform.tfstate`, or `terra-key-ec2` to GitHub.  
- Always use **.gitignore** to keep secrets safe.

---

## ✍️ Author

**Akshith**  
Terraform + AWS + Nginx automation demo project 🌍

---

## ⭐ Support

If you found this project helpful:

- ⭐ Give it a star on GitHub  
- 🍴 Fork and share it for learning!

---
